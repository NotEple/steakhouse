import {aboutType} from './aboutType'
import {frontType} from './frontType'
import {logoType} from './logoType'
import {menuType} from './menuType'

export const schemaTypes = [menuType, logoType, aboutType, frontType]
